CREATE TABLE [tmp].[item_attribute_temp] (
    [company_id] VARCHAR (MAX) NULL,
    [item_no]    VARCHAR (MAX) NULL,
    [cdc_key]    BIGINT        NULL,
    [attribute]  VARCHAR (MAX) NULL,
    [value]      VARCHAR (MAX) NULL
);

